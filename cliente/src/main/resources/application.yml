server:
  port: ${PORT:0}

spring:
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: create-drop
  datasource:
    platform: postgres
    url: jdbc:postgresql://localhost:5432/cliente
    username: postgres
    password: postgres
    driverClassName: org.postgresql.Driver